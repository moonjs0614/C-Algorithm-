//[main 함수] main함수에서는 KFC 함수를 호출합니다. 그 외 동작은 없습니다.

//[KFC 함수] 아래와 같은 세 가지 동작을 수행합니다.
//chicken 함수를 호출 후 리턴 받은 값을 저장합니다.
//coke 함수를 호출 후 리턴 받은 값을 저장합니다.
//두 리턴 받은 값을 출력합니다.

//[chicken 함수] 숫자 하나를 입력받고, 입력 받은 숫자에 10을 더한 값을 리턴합니다.

//[coke 함수] 문자 하나를 입력받고, 입력 받은 문자를 그대로 리턴합니다.

#include <iostream>

int a;
char b;

int Chicken()
{
  std::cin >> a;
  return a+10;
}

char Coke()
{
  std::cin >> b;
  return b;
}

void KFC()
{
  std::cout << Chicken();
  std::cout << Coke();
}

int main(){
  
  KFC();
  
  return 0;}
