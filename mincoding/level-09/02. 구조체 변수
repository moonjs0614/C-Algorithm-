//int x, int y가 들어가 있는 구조체ABC를정의하고,
//ABC 구조체 변수t를선언 해 주세요
//t.x와 t.y에 두 수를 입력받고 합을 출력 해 주세요

#include <iostream>

int main(){
  
  struct ABC
  {
    int x;
    int y;
  };
  
  ABC t;
  
  std::cin >> t.x >> t.y;
  
  int sum = t.x + t.y;
  
  std::cout << sum;
  
  return 0;}
