//숫자 1개를 입력 받으세요
//그 숫자에 2를 곱하고, 20을 더하고, 다시 5로 나눈 값이 100과같지 않으면 (!=)
//"Magic"이라는 글씨를 출력 하세요.

#include <stdio.h>

int main(){
  
  int a;
  scanf("%d", &a);
  
  int b;
  b = (((a * 2) + 20) / 5);
  
  if (b != 100)
  {
    printf("Magic");
  }
  
  return 0;}
